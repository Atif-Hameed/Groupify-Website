import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:{
        darkColor : {
            main : "#000"
        },
        brightColor:{
            main : '#fff'
        },
        borderColor:{
            main : '#E5E7EB'
        },
        secondary : {
            main : "#00778B" 
        }
    }
})

export default theme;